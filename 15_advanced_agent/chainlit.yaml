chainlit: 2.0.0

ui:
  name: "Advanced Agent"
  description: "OpenAI Agent SDK Advanced Agent"

default_expand_messages: true

auth:
  required: true
  providers:
    - github

oauth-providers:
  github:
    client_id: ${OAUTH_GITHUB_CLIENT_ID}
    client_secret: ${OAUTH_GITHUB_CLIENT_SECRET}